apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "plumber.fullname" . }}-test-connection"
  labels:
    {{- include "plumber.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "plumber.fullname" . }}:{{ .Values.service_http_api.port }}']
  restartPolicy: Never
